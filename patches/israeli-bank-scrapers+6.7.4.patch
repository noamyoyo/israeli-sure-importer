diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
index 4e821fa..f47be55 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
@@ -58,8 +58,14 @@ function createLoginFields(credentials) {
   }];
 }
 function getAmountData(amountStr) {
-  let amountStrCopy = amountStr.replace(_constants.SHEKEL_CURRENCY_SYMBOL, '');
+  let amountStrCopy = (amountStr || '').replace(_constants.SHEKEL_CURRENCY_SYMBOL, '');
+  // Normalise Unicode minus (U+2212) to ASCII '-' (defensive)
+  amountStrCopy = amountStrCopy.replace(/−/g, '-');
   amountStrCopy = amountStrCopy.replaceAll(',', '');
+  // Strip ALL whitespace, including the space the bank inserts between the minus
+  // sign and the digits for negative balances ("- 20,926.69"). Without this,
+  // parseFloat stops at that space and returns NaN for any negative amount.
+  amountStrCopy = amountStrCopy.replace(/\s/g, '');
   return parseFloat(amountStrCopy);
 }
 function getTxnAmount(txn) {
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
index ab8e48d..8b3a782 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
@@ -117,7 +117,8 @@ function getTransactionType(planName, planTypeId) {
         case 5:
           return _transactions2.TransactionTypes.Normal;
         default:
-          throw new Error(`Unknown transaction type ${cleanedUpTxnTypeStr}`);
+          console.warn(`Unknown transaction type ${cleanedUpTxnTypeStr} - treating as Normal`);
+          return _transactions2.TransactionTypes.Normal;
       }
   }
 }
@@ -217,7 +218,17 @@ function prepareTransactions(txns, startMoment, combineInstallments, enableTrans
     clonedTxns = (0, _transactions.fixInstallments)(clonedTxns);
   }
   clonedTxns = (0, _transactions.sortTransactionsByDate)(clonedTxns);
-  clonedTxns = enableTransactionsFilterByDate ? (0, _transactions.filterOldTransactions)(clonedTxns, startMoment, combineInstallments || false) : clonedTxns;
+  if (enableTransactionsFilterByDate) {
+    clonedTxns = clonedTxns.filter(txn => {
+      // For recurring loans the purchaseDate is months/years in the past; use
+      // processedDate (bank charge date) when the gap exceeds 100 days so these
+      // installments are not dropped by the start-date filter.
+      const dateToCheck = txn.processedDate &&
+        new Date(txn.processedDate) - new Date(txn.date) > 100 * 86400 * 1000
+          ? txn.processedDate : txn.date;
+      return startMoment.isSameOrBefore(dateToCheck);
+    });
+  }
   return clonedTxns;
 }
 async function fetchTransactions(page, options) {
